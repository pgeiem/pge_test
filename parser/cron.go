package parser

/*
func ParseCronPattern(pattern string, now time.Time) error {

	gron := gronx.New()
	if !gron.IsValid(pattern) {
		return fmt.Errorf("Invalid date pattern: %s", pattern)
	}
	gron.IsDue(pattern, now)
}
*/
